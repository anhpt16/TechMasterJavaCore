package states;

import javax.swing.*;

public class DefaultState extends GhostState{
    public DefaultState(ImageIcon imageIcon, int speedGhost, long seconds) {
        super(imageIcon, speedGhost, seconds);
    }
}
